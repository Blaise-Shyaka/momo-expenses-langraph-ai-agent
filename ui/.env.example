AGENT_URL=http://localhost:8123

AUTH_SERVICE_URL=http://localhost:8001

AUTH_SECRET=

# Trust the reverse proxy's forwarded headers so next-auth's own redirects
# resolve to the real origin instead of falling back to localhost:3000.
AUTH_TRUST_HOST=true
